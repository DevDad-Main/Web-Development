PORT=3000
# * -> Allow everything, not secure
CORS_ORIGIN=*

# Add the front end url to make it more secure
# Allowing all origins with app.use(cors()) is fine in development,
# but in production, it's best to only allow trusted domains to prevent abuse or data leaks.

#CORS_ORIGIN=http://localhost:3000

MONGODB_URL=mongodb+srv://youtubeuser:youtubeuser123@cluster1.6zhu8cq.mongodb.net
